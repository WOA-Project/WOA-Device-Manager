<Page
    x:Class="WOADeviceManager.Pages.zDebugPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WOADeviceManager.Pages"
    xmlns:manager="using:WOADeviceManager.Managers"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <StackPanel Padding="16" Spacing="10">
            <StackPanel Orientation="Horizontal" Spacing="4">
                <Button Click="RebootToBootloader_Click" IsEnabled="{x:Bind manager:DeviceManager.Device.IsADBCompatible}">🔄️ Reboot to Bootloader</Button>
                <Button Click="RebootToAndroid_Click" IsEnabled="{x:Bind manager:DeviceManager.Device.BootloaderConnected}">🔄️ Reboot to Android</Button>
                <Button Click="BootTWRP_Click" IsEnabled="{x:Bind manager:DeviceManager.Device.BootloaderConnected}">🔄️ Reboot to TWRP</Button>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Spacing="4">
                <Button Click="FlashUnlock_Click" IsEnabled="{x:Bind manager:DeviceManager.Device.BootloaderConnected}">🔓 Flash Unlock ⚠️</Button>
                <Button Click="FlashLock_Click" IsEnabled="{x:Bind manager:DeviceManager.Device.BootloaderConnected}">🔒 Flash Lock ⚠️</Button>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Spacing="4">
                <Button Click="PushParted_Click" IsEnabled="{x:Bind manager:DeviceManager.Device.TWRPConnected}">📲 Push parted</Button>
                <Button Click="MassStorageMode_Click" IsEnabled="{x:Bind manager:DeviceManager.Device.TWRPConnected}">📲 Enable Mass Storage Mode</Button>
            </StackPanel>
            <TextBlock x:Name="DebugInfo" TextWrapping="Wrap"></TextBlock>
        </StackPanel>
    </Grid>
</Page>
